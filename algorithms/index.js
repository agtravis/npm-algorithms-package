'use strict';

const preClassDrills = require(`./preClassDrills`);
const classAlgorithms = require(`./classAlgorithms`);
const codeWars = require(`./codeWars`);
const binarySearch = require(`./binarySearch`);
const leetCode = require(`./leetCode`);

module.exports = {
  preClassDrills,
  classAlgorithms,
  codeWars,
  binarySearch,
  leetCode,
};
